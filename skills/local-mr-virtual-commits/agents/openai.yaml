interface:
  display_name: "Local MR Virtual Commits"
  short_description: "Plan and confirm guided virtual-commit reviews"
  default_prompt: "Use $local-mr-virtual-commits to plan this local diff as virtual commits and ask for my confirmation before creating the review."
