interface:
  display_name: "Local MR Virtual Commits"
  short_description: "Turn large local diffs into guided review commits"
  default_prompt: "Use $local-mr-virtual-commits to organize this local diff into a human-friendly virtual commit review."
